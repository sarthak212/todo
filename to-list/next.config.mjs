/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "cdn-icons-png.flaticon.com",
      "avatar.iran.liara.run",
    ],
  },
};

export default nextConfig;
